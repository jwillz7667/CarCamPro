{
  "$schema": "https://railway.com/railway.schema.json",
  "_comment": "Worker service config — point the worker Railway service's Config Path (Service → Settings → Config-as-Code → Config Path) at backend/railway.worker.json, OR supply these settings via the Railway dashboard instead.",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "docker/Dockerfile.worker"
  },
  "deploy": {
    "startCommand": "npm run railway:worker",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "numReplicas": 1
  }
}
